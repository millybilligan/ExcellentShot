# ExcellentShot
A better version of NiceShot with same functions.

## Functionality:
* Get the client username
* Get the client windows version
* Get the main disk (not stable)
* Get the reverse shell
* Reconnect client to server if it fail
